name: CI for KernelCI Dashboard

on:
  push:
    branches: [main]
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

concurrency:
  group: ci-check-${{ github.ref }}
  cancel-in-progress: true

env:
  CI: true

jobs:
  deploy-staging:
    if: github.event_name == 'push' && github.tags == 'release/**'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to staging
        run: |
